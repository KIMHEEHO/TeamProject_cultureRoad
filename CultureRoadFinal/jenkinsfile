pipeline {
    agent any

    stages {

        stage('github-clone') {
            steps {
                git branch: '', credentialsId:'joy', url : '{REPOSITORY URL}'
            }
        }
    }
}